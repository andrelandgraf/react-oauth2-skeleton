import React from 'react';
import PropTypes from 'prop-types';

const DropDownAction = ( { link, children } ) => (
    <div />
);

DropDownAction.propTypes = {
    children: PropTypes.node.isRequired,
};

export default DropDownAction;
